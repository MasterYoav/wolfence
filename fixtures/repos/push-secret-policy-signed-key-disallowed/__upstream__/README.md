# Push Secret Signed Key Disallowed Fixture

This upstream snapshot contains the approver and key-id allowlist policy plus
the cryptographically valid but policy-disallowed signed receipt before the
secret-bearing commit is added.
